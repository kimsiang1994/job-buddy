# Secrets — never commit real credentials
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# OS / editor cruft
Thumbs.db
Desktop.ini
.DS_Store
.vscode/
.idea/

# Local run artefacts
# usage_log.jsonl is per-machine measurement data, not shared state.
usage_log.jsonl
*.tmp
# Downloaded by fetch_tokenizer.py; several MB, reproducible on demand.
tokenizer/
