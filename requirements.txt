# OPTIONAL. Everything in this repo runs on the Python standard library alone.
#
# Installing this only upgrades token counting from DeepSeek's documented
# char-ratio heuristic (0.3 tok/char EN, 0.6 tok/char CJK) to exact counts:
#
#   py fetch_tokenizer.py            # downloads tokenizer/tokenizer.json
#   pip install -r requirements.txt
#
# `tokenizers` is the lightweight Rust-backed loader; the full `transformers`
# stack is not needed just to read a tokenizer.json.
#
# Before keeping this dependency, check it is actually earning its place:
#   py calibrate_budgets.py --report
# prints the measured error of each estimator against real API token counts.
tokenizers>=0.15
